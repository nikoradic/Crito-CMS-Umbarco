@using Umbraco.Cms.Web.Common.PublishedModels;
@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<ContentModels.HomePage>
@using ContentModels = Umbraco.Cms.Web.Common.PublishedModels;

<section class="service-section">
    
        <div class="section-title">@Model.Value("ourServicesSectionTitle")</div>
    <div class="container">
        <h2>@Model.Value("ourServicesSectionHeading")</h2>
        <div class="service-boxes">
            @Html.GetBlockListHtml(Model.OurServices)
        </div>
    </div>
        <div class="browse-services">
            <a class="btn-outline-dark" href="@Model.Value("ourServicesSectionButtonUrl")">@Model.Value("ourServicesSectionButtonText")<i class="fa-solid fa-arrow-right"></i></a>
        </div>
</section>
